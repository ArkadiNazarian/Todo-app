import { Box } from "@mui/system";
import { NavBar } from "../NavBar";
import { SideBar } from "../SideBar";

export const View=()=>(
    <Box>
        <NavBar/>
        <Box>
            <SideBar/>
        </Box>
    </Box>
)